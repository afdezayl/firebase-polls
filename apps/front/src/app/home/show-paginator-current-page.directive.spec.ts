import { ShowPaginatorCurrentPageDirective } from './show-paginator-current-page.directive';

describe('ShowPaginatorCurrentPageDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPaginatorCurrentPageDirective();
    expect(directive).toBeTruthy();
  });
});
